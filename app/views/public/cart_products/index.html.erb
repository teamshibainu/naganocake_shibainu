<<<<<<< HEAD
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2><i class="fas fa-shopping-cart"></i>ショッピングカート</h2>
      <%= link_to "カートを空にする", @cart_product, method: :destroy_all, data: { confirm: 'カートを空にしますか？'}, class: 'btn' %>
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th>商品名</th>
            <th>単価(税込)</th>
            <th>数量</th>
            <th>小計</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @cart_product.cart_products.each do |product| %>
            <tr>
              <td>
                <%= link_to public_product_path(product) do %>
                  <%= attachment_image_tag product, :image, form: 'jpeg', size:"50x50" %>
                <% end %>
              </td>
              <td>
                <%= product.price %>円
              </td>
              <td>
                <%= product.quantity %>
              </td>
              <td>
                <%= product.price*product.quantity %>
              </td>
              <td>
                <%= link_to "削除する", product_path(product.id), method: :delete, class:"btn btn-sm btn-danger" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= link_to "買い物を続ける", publoc_root, class:"btn" %>
# 合計金額
    </div>
  </div>
</div>
=======
<div class="container m-5">
  <div class="row">
    <div class="col-md-3"></div>
    <h4 class="my-4"><span style="background-color:#EFEFEF">　ショッピングカート　</span></h4>
    <% if @cart_products.exists? %>
    <table class="table table-hover table-inverse">
      <thead>
        <th>商品名</th>
        <th>単価（税込）</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
        <th colspan="5"></th>
      </thead>
      <tbody>
        <% @cart_products.each do |cart_product| %>
        <tr>
          <td><%= cart_product.product.name %></td>
          <td><%= tax_price(cart_product.product.price).to_s(:delimited) %></td>
          <td>
            <%= form_with model: cart_product, url: public_cart_product_path(cart_product), local:true do |f| %>
              <%= f.select :quantity, options_for_select((1..10).to_a, selected: cart_product.quantity) %>
              <%= f.submit '変更', class: 'btn btn-success' %>
            <% end %>
          </td>
          <td><%= ((cart_product.product.price * 1.10).to_i * (cart_product.quantity).to_i).to_s(:delimited) %></td>
          <td><%= link_to "削除する", public_cart_product_path(cart_product.id), method: :delete, class: 'btn btn-danger' %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <% else %>
      <br><br>
      <h4>まだジャンルが追加されていません。</h4>
    <% end %>
    <%= link_to "情報入力に進む", new_public_order_path %>
  </div>
>>>>>>> origin/develop
